# 軟片模擬預設管理系統依賴
# Film Simulation Preset Manager Dependencies

# 核心圖像處理
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# GUI界面 (可選)
PyQt5>=5.15.0

# JSON處理 (Python內建，無需額外安裝)
# pathlib (Python內建，無需額外安裝)